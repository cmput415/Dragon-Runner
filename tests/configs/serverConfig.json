{
  "name": "Server Config",
  "testDir": "../packages/CPackage",
  "testedExecutablePaths": {
    "gcc": "/usr/bin/gcc"
  },
  "toolchains": {
    "gcc-explorer": [
      {
        "exe": "$EXE",
        "args": [
          "-xc",
          "$INPUT",
          "-o",
          "$OUTPUT"
        ],
        "allowError": true
      },
      {
        "exe": "$INPUT",
        "usesInStr": true,
        "allowError": true
      }
    ]
  }
}
