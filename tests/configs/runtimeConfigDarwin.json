{
  "testDir": "../packages/RuntimeCPackage",
  "testedExecutablePaths": {
    "gcc": "gcc"
  },
  "runtimes": {
    "gcc": "../lib/libfib.dylib"
  },
  "toolchains": {
    "clang-runtime": [
      {
        "exe": "$EXE",
        "args": [
          "-c",
          "$INPUT",
          "-o",
          "$OUTPUT"
        ]
      },
      {
        "exe": "$EXE",
        "args": [
          "$INPUT",
          "-o",
          "$OUTPUT",
          "-L$RT_PATH",
          "-l$RT_LIB"
        ]
      },
      {
        "exe": "$INPUT",
        "usesInStr": true,
        "allowError": true,
        "usesRuntime": true
      }
    ]
  }
}
